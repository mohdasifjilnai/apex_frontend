import { RemoveZeroFromStartingDirective } from './remove-zero-from-starting.directive';

describe('RemoveZeroFromStartingDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveZeroFromStartingDirective();
    expect(directive).toBeTruthy();
  });
});
