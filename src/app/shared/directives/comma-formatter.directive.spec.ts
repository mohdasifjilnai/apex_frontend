import { CommaFormatterDirective } from './comma-formatter.directive';

describe('CommaFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new CommaFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
