import { NumberWithDecimalDirective } from './number-with-decimal.directive';

describe('NumberWithDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberWithDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
