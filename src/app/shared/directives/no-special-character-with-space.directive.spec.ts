import { NoSpecialCharacterWithSpaceDirective } from './no-special-character-with-space.directive';

describe('NoSpecialCharacterWithSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSpecialCharacterWithSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
