import { RegistrationNumberDirective } from './registration-number.directive';

describe('RegistrationNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new RegistrationNumberDirective();
    expect(directive).toBeTruthy();
  });
});
