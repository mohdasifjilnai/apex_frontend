import { SpecialCharacterNotAllowedDirective } from './special-character-not-allowed.directive';

describe('SpecialCharacterNotAllowedDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecialCharacterNotAllowedDirective();
    expect(directive).toBeTruthy();
  });
});
