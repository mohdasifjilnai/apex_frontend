import { NotAllowDotDirective } from './not-allow-dot.directive';

describe('NotAllowDotDirective', () => {
  it('should create an instance', () => {
    const directive = new NotAllowDotDirective();
    expect(directive).toBeTruthy();
  });
});
