import { RemoveNewlinesDirective } from './remove-newlines.directive';

describe('RemoveNewlinesDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveNewlinesDirective();
    expect(directive).toBeTruthy();
  });
});
