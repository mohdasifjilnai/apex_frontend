import { NoMaskedValueDirective } from './no-masked-value.directive';

describe('NoMaskedValueDirective', () => {
  it('should create an instance', () => {
    const directive = new NoMaskedValueDirective();
    expect(directive).toBeTruthy();
  });
});
